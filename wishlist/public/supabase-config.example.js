// Template for local development: copy to `public/supabase-config.js` and DO NOT commit.
// Example:
// window.SUPABASE_ENABLED = true;
// window.supabaseClient = supabase.createClient("https://xyz.supabase.co", "public-anon-key");

// For production/CI we inject `public/supabase-config.js` during deploy from GitHub Secrets.
// The deploy workflow generates the file from `SUPABASE_URL` and `SUPABASE_ANON_KEY` secrets.
// `public/supabase-config.js` is gitignored by default so it won't be committed.
