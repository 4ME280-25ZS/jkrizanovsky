# DISABLED: wishlist deploy workflow removed
# This workflow was disabled as the wishlist subproject has been removed from the repository.
# Keeping the file as a record; no jobs will run.

on: []

jobs: {}
