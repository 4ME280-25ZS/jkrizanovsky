// Local template (DO NOT commit a file with real keys)
// Copy to `public/supabase-config.js` for local testing
// Example:
// window.SUPABASE_ENABLED = true;
// window.supabaseClient = supabase.createClient("https://xyz.supabase.co", "public-anon-key");

// In CI we will generate `public/supabase-config.js` from GitHub Secrets.